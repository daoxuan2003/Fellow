# Active Work

Last updated: 2026-08-21

This file is short-lived project memory. Update it whenever work remains
unfinished. Remove completed task detail after the PR is merged, but preserve
durable decisions in ADRs or contracts.

## Repository state observed

- **VERIFIED:** production release `v8.4.1`, `origin/main` and
  `origin/develop` resolve to `2e769096bf0ef40d4d5b680d4a6acc2fde4b11b6`.
- **VERIFIED:** the worktree was clean before creating the hotfix branch.

## Current task

- Primary manifest:
  `.ai/tasks/task-postgraduate-daily-task-timestamp-conflict.json`; stage:
  `validating`.
- Topic branch: `fix/postgraduate-daily-task-timestamps`.
- Goal: stop Mongoose from injecting a second `updatedAt` operator into the
  idempotent daily-task bulk upsert, then release the verified fix as `v8.4.2`.
- **VERIFIED:** the product owner supplied a production `MongoBulkWriteError`
  code `40`: manual `$setOnInsert.updatedAt` conflicts with the schema timestamp
  update generated by Mongoose.
- **VERIFIED:** local Mongoose supports operation-level
  `updateOne.timestamps=false`; this prevents framework injection without
  changing the schema or any other write path.
- Preserved behavior: JWT couple scope, local day, requestId/position unique
  idempotency, insert timestamps, write-before-sync/push, batch limits and UI.
- Intended difference: each daily-task bulk upsert disables Mongoose timestamp
  injection while retaining one shared manual `createdAt`/`updatedAt` only on
  first insert.
- **UNKNOWN:** how many production attempts hit the failure; no user data read
  or migration is necessary for the fix.
- **VERIFIED:** the product owner explicitly authorized direct release; target
  is `v8.4.2` and rollback target is `v8.4.1`.

## Validation pending

- **Passed:** focused backend regression 11/11, including the public Mongoose
  bulkWrite path reaching the mocked Mongo driver without a duplicate
  `updatedAt` operator.
- **Passed:** complete frontend tests 156/156; backend verification checked 111
  JavaScript files, passed 288/288 tests and found 0 production dependency
  vulnerabilities.
- **Passed:** project context, design contract, work-item and diff checks pass;
  JWT scope, idempotency, insert timestamps and side-effect ordering are
  unchanged.
- Pending: topic branch commit/push with exact-head remote Test and AI
  Governance.
- Pending: pre-release backup, scoped release gate, v8.4.2 tag/deployment and
  production SHA, WebSocket and API health evidence.
